diff --git a/.github/workflows/build-kernel-release.yml b/.github/workflows/build-kernel-release.yml
index 3c3b492..e6e0672 100644
--- a/.github/workflows/build-kernel-release.yml
+++ b/.github/workflows/build-kernel-release.yml
@@ -146,6 +146,7 @@ jobs:
       susfs_hash_android16_6_12: ${{ steps.set-matrix.outputs.susfs_hash_android16_6_12 }}
       ksu_resolved_hash:    ${{ steps.set-matrix.outputs.ksu_resolved_hash }}
       ksu_options_normalized: ${{ steps.set-matrix.outputs.ksu_options_normalized }}
+      susfs_base_version: ${{ steps.set-matrix.outputs.susfs_base_version }}
     env:
       GH_TOKEN: ${{ github.token }}
       GH_HTTP_TIMEOUT: 600
@@ -323,10 +324,8 @@ jobs:
           # Inject ksu_resolved_hash into each device in the matrix
           merged_matrix=$(echo "$merged_matrix" | jq  --arg resolved_sha "$resolved_sha" 'map(.ksu_resolved_hash = $resolved_sha)')
           
-          # SUSFS hash fetch
-          SUSFS_REPO="https://gitlab.com/simonpunk/susfs4ksu.git"
+          # SUSFS hash resolution (Stable Beta Logic)
           PROJECT_ID="simonpunk%2fsusfs4ksu"
-          
           declare -A default_branches=(
             ["android12-5.10"]="gki-android12-5.10"
             ["android13-5.15"]="gki-android13-5.15"
@@ -334,7 +333,6 @@ jobs:
             ["android15-6.6"]="gki-android15-6.6"
             ["android16-6.12"]="gki-android16-6.12"
           )
-          
           declare -A user_inputs=(
             ["android12-5.10"]="${{ inputs.android12-5_10_susfs_branch_or_commit }}"
             ["android13-5.15"]="${{ inputs.android13-5_15_susfs_branch_or_commit }}"
@@ -343,50 +341,29 @@ jobs:
             ["android16-6.12"]="${{ inputs.android16-6_12_susfs_branch_or_commit }}"
           )
           
-          mapfile -t active_keys < <(
-            echo "$merged_matrix" \
-              | jq -r '.[] | select(.susfs == true) | "\(.android_version)-\(.kernel_version)"' \
-              | sort -u
-          )
-          
+          mapfile -t active_keys < <(echo "$merged_matrix" | jq -r '.[] | select(.susfs == true) | "\(.android_version)-\(.kernel_version)"' | sort -u)
           echo "≡ƒöì Resolving SUSFS hashes for: ${active_keys[*]:-none}"
           
           declare -A susfs_hashes
           for key in "${active_keys[@]}"; do
             default_val="${default_branches[$key]}"
             user_val="${user_inputs[$key]:-$default_val}"
-            
-            MAX_RETRIES=3
-            RETRY_COUNT=0
-            RETRY_DELAY=5
-            resolved=""
-            
-            until [ $RETRY_COUNT -ge $MAX_RETRIES ]; do
-              # We query the commit endpoint which accepts Branch, Tag, or SHA
-              RESULT=$(curl -s --fail "https://gitlab.com/api/v4/projects/${PROJECT_ID}/repository/commits/${user_val}")
-              EXIT_CODE=$?
-              
-              if [ $EXIT_CODE -eq 0 ] && [ -n "$RESULT" ]; then
-                resolved=$(echo "$RESULT" | jq -r '.id')
-                echo "  Γ£à Query for $key input field Success"
-                break
-              fi
-              
-              RETRY_COUNT=$((RETRY_COUNT + 1))
-              echo "::warning::GitLab API failed (Attempt $RETRY_COUNT/$MAX_RETRIES). Retrying..."
-              sleep $RETRY_DELAY
-            done
-            
-            # Final Validation
-            if [ -z "$resolved" ] || [ "$resolved" = "null" ]; then
-               echo "::error::Could not resolve SUSFS ref '$user_val' for $key on GitLab."
-               exit 1
-            fi
-            
+            RESULT=$(curl -sf "https://gitlab.com/api/v4/projects/${PROJECT_ID}/repository/commits/${user_val}" || echo "{}")
+            resolved=$(echo "$RESULT" | jq -r '.id // empty')
+            if [ -z "$resolved" ]; then echo "::error::Could not resolve SUSFS $user_val for $key"; exit 1; fi
             susfs_hashes["$key"]="$resolved"
-            echo "  Γ£à Resolved: $key ΓåÆ $resolved"
+            echo "Γ£à Resolved: $key -> $resolved"
           done
           
+          # Write outputs for workflow synchronization
+          echo "active_gki_keys=$(IFS=','; echo "${active_keys[*]:-}")" >> "$GITHUB_OUTPUT"
+          echo "susfs_hash_android12_5_10=${susfs_hashes[android12-5.10]:-unknown}" >> "$GITHUB_OUTPUT"
+          echo "susfs_hash_android13_5_15=${susfs_hashes[android13-5.15]:-unknown}" >> "$GITHUB_OUTPUT"
+          echo "susfs_hash_android14_6_1=${susfs_hashes[android14-6.1]:-unknown}"   >> "$GITHUB_OUTPUT"
+          echo "susfs_hash_android15_6_6=${susfs_hashes[android15-6.6]:-unknown}"   >> "$GITHUB_OUTPUT"
+          echo "susfs_hash_android16_6_12=${susfs_hashes[android16-6.12]:-unknown}" >> "$GITHUB_OUTPUT"
+          echo "susfs_base_version=v2.1.0" >> "$GITHUB_OUTPUT"
+          
           # Write CSV list of active GKI versions
           active_gki_keys_csv=$(IFS=','; echo "${active_keys[*]:-}")
           echo "active_gki_keys=$active_gki_keys_csv" >> "$GITHUB_OUTPUT"
@@ -888,7 +865,7 @@ jobs:
             feature_flags=$(jq -r --arg MODEL "$model" '
               .[] | 
               select(.model == $MODEL) |
-              "\(.hmbird // false)\t\(.susfs // false)\t\(.bbr // false)\t\(.bbg // false)\t\(.ttl // false)\t\(.ip_set // false)\t\(.unicode // false)"
+              "\(.hmbird // false)\t\(.susfs // false)\t\(.bbr // false)\t\(.bbg // false)\t\(.ttl // false)\t\(.ip_set // false)\t\(.unicode // false)\t\(.ds // false)\t\(.battery_opt // false)"
             ' <<< "$JSON_BUILD_DATA")
             
             features=""
@@ -896,21 +873,34 @@ jobs:
             if [[ -z "$feature_flags" ]]; then
               echo "Warning: No JSON entry found for model: $model" >&2
             else
-              IFS=$'\t' read -r hmbird susfs bbr bbg ttl ip_set unicode <<< "$feature_flags"
+              IFS=$'\t' read -r hmbird susfs bbr bbg ttl ip_set unicode ds battery_opt <<< "$feature_flags"
               
-              # Build features string based on boolean values
-              [[ "$hmbird" == "true" ]] && features+="≡ƒÉª HMBIRD "
-              [[ "$susfs" == "true" ]] && features+="α╢₧ SusFS "
+              # Build features string based on boolean values - Unique Icons & Strict Applicability
+              [[ "$susfs" == "true" ]] && features+="≡ƒ¢í∩╕Å SusFS "
+              [[ "$hmbird" == "true" ]] && features+="≡ƒªï HMBird "
               [[ "$bbr" == "true" ]] && features+="≡ƒÜÇ BBR "
-              [[ "$bbg" == "true" ]] && features+="≡ƒ¢í∩╕Å BBG "
+              [[ "$bbg" == "true" ]] && features+="≡ƒº▒ BBG "
               [[ "$ttl" == "true" ]] && features+="≡ƒîÉ TTL "
-              [[ "$ip_set" == "true" ]] && features+="≡ƒöº IP_SET & IPv6 NAT "
-              [[ "$unicode" == "true" ]] && features+="≡ƒöº Unicode Fix "
-              [[ "${{ inputs.enable_ntsync }}" == "true" ]] && [[ "$kernel_ver" == "6.1"* || "$kernel_ver" == "6.6"* || "$kernel_ver" == "6.12"* ]] && features+="≡ƒò╣∩╕Å NTSync "
+              [[ "$ip_set" == "true" ]] && features+="≡ƒôí IPSET "
+              [[ "$unicode" == "true" ]] && features+="≡ƒöº Unicode "
+              [[ "$ds" == "true" ]] && features+="≡ƒîî Droidspaces "
+              [[ "$battery_opt" == "true" ]] && features+="≡ƒöï Battery "
+              
+              # NTSync strict applicability: Only for Linux 6.1, 6.6, 6.12 if enabled in inputs
+              if [[ "${{ inputs.enable_ntsync }}" == "true" ]]; then
+                if [[ "$kernel_ver" == "6.1"* || "$kernel_ver" == "6.6"* || "$kernel_ver" == "6.12"* ]]; then
+                  features+="≡ƒò╣∩╕Å NTSync "
+                fi
+              fi
+              
+              # WireGuard is standard integrated feature
               features+="≡ƒ¢é WireGuard "
             fi
             
-            printf "| %-13s | %-10s | %-16s | %-30s |\n" \
+            # Remove any trailing spaces and clean up
+            features=$(echo "$features" | sed 's/ *$//')
+            
+            printf "| %-13s | %-10s | %-16s | %s |\n" \
               "$model" "$os_ver" "$kernel_ver" "$features" >> release_notes.md
           done
           
